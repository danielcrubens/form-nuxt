specVersion: "v1beta"
serviceId: "nuxt-app"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "nuxt-app-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
